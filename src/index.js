import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <div>
    <h1>Hello, World!</h1>
    <h3>Study React</h3>
    <p>Day 1, ReactDOM, render()</p>
  </div>,
  document.getElementById("root")
);
